<Window x:Class="SudokuSolver.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SudokuSolver.View"
        xmlns:controls="clr-namespace:SudokuSolver.View.CustomControls"
        mc:Ignorable="d"
        Title="Sudoku Solver" Height="450" Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" BorderBrush="Black" BorderThickness="1.2"  Margin="10">
            <controls:SudokuBoard x:Name="SudokuBoard" Grid.Column="0" Grid.Row="0"/>
        </Border>
        <Grid Grid.Column="1" Margin="0,11,5,11">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.145*"/>
                <RowDefinition Height="0.145*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button x:Name="solveBtn" Grid.Row="0" Margin="2" Content="Solve Sudoku" FontSize="16"/>
            <Button x:Name="clearBtn" Grid.Row="1" Margin="2" Content="Clear" FontSize="16"/>
        </Grid>
    </Grid>
</Window>